/** Visible site title (keep in sync with root layout metadata.title). */
export const SITE_TITLE = 'Anyway, I Shipped It';

/** Placeholder tagline — replace when you have final copy. */
export const SITE_TAGLINE = 'A blog by Helen.';
